name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Determine version from tag
        run: echo "PLUGIN_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Verify tag matches gradle.properties
        run: |
          # gradle.properties has CRLF line endings; strip \r (and any other whitespace)
          # or the comparison silently fails even when both sides look identical in logs.
          PROP_VERSION="$(grep -m1 '^pluginVersion=' gradle.properties | cut -d= -f2 | tr -d '\r\n ')"
          if [ "$PLUGIN_VERSION" != "$PROP_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME (version $PLUGIN_VERSION) does not match pluginVersion=$PROP_VERSION in gradle.properties"
            exit 1
          fi

      - name: Cache Gradle
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/notifications
            ~/.gradle/native
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run tests
        run: gradle --no-daemon test

      - name: Build plugin
        run: gradle --no-daemon buildPlugin

      - name: Verify plugin (JetBrains Plugin Verifier)
        run: gradle --no-daemon verifyPlugin

      - name: Sign plugin
        env:
          CERTIFICATE_CHAIN: ${{ secrets.CERTIFICATE_CHAIN }}
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          PRIVATE_KEY_PASSWORD: ${{ secrets.PRIVATE_KEY_PASSWORD }}
        run: gradle --no-daemon signPlugin

      - name: Extract changelog section for this version
        run: |
          awk -v ver="$PLUGIN_VERSION" '
            /^## \[/ {
              if (found) exit
              if (index($0, "[" ver "]") > 0) { found=1; next }
              next
            }
            found { print }
          ' CHANGELOG.md > release_notes.md
          if [ ! -s release_notes.md ]; then
            echo "See CHANGELOG.md for details." > release_notes.md
          fi

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            build/distributions/*-signed.zip \
            --title "$GITHUB_REF_NAME" \
            --notes-file release_notes.md
